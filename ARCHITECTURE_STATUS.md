# T-Designer 架构现状图

## 整体架构层次

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户界面层 (UI)                           │
├─────────────────────────────────────────────────────────────────┤
│  dialogUnitAttr        │  FunctionManager   │  DMatrixViewer    │
│  ✅ T语言编辑完整      │  ⚠️ 基础框架       │  ✅ 查看器完整     │
│  ✅ 端口配置完整       │  ❌ 变量范围缺失   │  ❌ 集成缺失       │
│                        │                    │                    │
│  dialogsymbolattribute │  SelectFunction    │  TestManagement   │
│  ❌ T语言编辑缺失      │  (T-Solver遗留)    │  ⚠️ 未连接D矩阵   │
│  ❌ 端口配置缺失       │                    │                    │
└─────────────────────────────────────────────────────────────────┘
                                 ↕
┌─────────────────────────────────────────────────────────────────┐
│                       业务逻辑层 (BO)                            │
├─────────────────────────────────────────────────────────────────┤
│  Container Layer (容器层) - ⚠️ 部分实现                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ ContainerRepository      ✅ 数据访问完整                  │  │
│  │ ContainerEntity          ✅ 数据对象完整                  │  │
│  │ ContainerBehaviorAdapter ❌ 缺失 [P0]                    │  │
│  │ BehaviorAggregator       ⚠️ 存在但功能不明               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  System Modeling (系统建模) - ❌ 关键缺失                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ SystemModelAssembler     ❌ 缺失 [P0]                    │  │
│  │ ConnectSugarGenerator    ❌ 缺失 [P0]                    │  │
│  │ SystemEntity             ✅ 求解核心完整                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  Function Management (功能管理) - ⚠️ 部分实现                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ FunctionRepository          ✅ 数据访问完整               │  │
│  │ FunctionAnalysisService     ⚠️ 基础实现                  │  │
│  │ SystemStructureService      ⚠️ 裁剪基础                  │  │
│  │ FunctionDependencyResolver  ⚠️ 基础实现                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  Validation (校验) - ✅ 完整实现                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ SmtSyntaxChecker         ✅ Z3 语法校验                   │  │
│  │ TModelValidator          ✅ 端口一致性校验                │  │
│  │ TModelCheckService       ✅ 统一校验服务                  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  Testability Analysis (测试性分析) - ✅ 核心完整                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ DMatrixBuilder           ✅ 构建算法完整                  │  │
│  │ SmtFacade                ✅ Z3 接口完整                   │  │
│  │ ConstraintUtils          ✅ 约束工具完整                  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                 ↕
┌─────────────────────────────────────────────────────────────────┐
│                       数据访问层 (DA)                            │
├─────────────────────────────────────────────────────────────────┤
│  Project Database (templete/project.db)                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Equipment.TModel            ✅ 13,860 条 (传统方式)       │  │
│  │ component_smt               ⚠️ 0 条 (未使用) [P0]        │  │
│  │ system_smt                  ⚠️ 0 条 (未使用) [P0]        │  │
│  │ port_config                 ✅ 15 条 (有数据)            │  │
│  │ port_connect_macro_family   ✅ 3 条 (内置宏族)           │  │
│  │ function_definition         ⚠️ 0 条 (框架就绪)           │  │
│  │ function_dependency         ⚠️ 0 条 (框架就绪)           │  │
│  │ function_io                 ⚠️ 0 条 (框架就绪)           │  │
│  │ dmatrix_meta                ⚠️ 0 条 (框架就绪)           │  │
│  │ container                   ⚠️ 表存在但未关联实体         │  │
│  │ equipment_containers        ❌ 0 条 (未使用)             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  Library Database (ref/LdMainData.db)                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Equipment.TModel            ✅ 13,860 条 (模板库)        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 数据流动现状

### 当前流程（部分断裂）

```
┌──────────────┐
│ 1. 元件插入   │
└──────┬───────┘
       │ ✅ Equipment 表插入
       │ ❌ 未创建容器
       │ ❌ 未生成 component_smt
       ↓
┌──────────────┐
│ 2. CAD连线   │
└──────┬───────┘
       │ ✅ JXB 表记录连线
       ↓
┌────────────────────┐
│ 3. 系统描述生成     │
│ buildAutoConns...  │
└──────┬─────────────┘
       │ ✅ 读取 Equipment.TModel
       │ ✅ 读取 JXB 连线
       │ ✅ 生成 DEF BEGIN...DEF END
       │ ⚠️ 硬编码 connect2e/3e
       │ ❌ 不读取 port_config 类型
       │ ❌ 不读取宏族定义
       ↓
┌──────────────────────────┐
│ textEditSystemDiscription │ ← 仅显示在界面
└──────────────────────────┘
       │
       │ ❌ 未保存到 system_smt 表
       │
       ↓
   [断裂点]
       │
       ↓
┌──────────────────┐
│ 4. 功能管理       │
│ FunctionManager  │
└──────┬───────────┘
       │ ⚠️ 手动传入 systemDescription 字符串
       │ ❌ 应从 system_smt 读取
       │ ✅ 保存到 function_definition
       │ ⚠️ 缺少变量范围配置
       ↓
┌──────────────┐
│ 5. D矩阵构建  │
│ DMatrixBuilder│
└──────┬───────┘
       │ ⚠️ 手动传入 systemDescription 字符串
       │ ❌ 应从 system_smt 读取
       │ ⚠️ 手动传入 functionInfoMap
       │ ❌ 应从 function_definition 读取
       │ ✅ 保存到 dmatrix_meta
       ↓
┌──────────────┐
│ 6. D矩阵查看  │
│ DMatrixViewer│
└──────────────┘
       │ ✅ 从 dmatrix_meta 读取
       │ ✅ 显示矩阵
       │ ❌ 未集成到测试管理
```

### 期望流程（完整闭环）

```
┌──────────────┐
│ 1. 元件插入   │
└──────┬───────┘
       │ ✅ Equipment 表插入
       │ [新增] 自动创建容器
       │ [新增] ContainerBehaviorAdapter
       ↓
┌──────────────────────────┐
│ 容器创建与SMT生成          │
│ ContainerBehaviorAdapter  │
└──────┬───────────────────┘
       │ ✅ 创建 container 记录
       │ ✅ 关联 equipment_containers
       │ ✅ 读取 Equipment.TModel
       │ ✅ 读取 port_config
       │ ✅ 生成容器级 SMT
       │ ✅ 保存到 component_smt (container_id)
       ↓
┌──────────────┐
│ 2. CAD连线   │
└──────┬───────┘
       │ ✅ JXB 表记录连线
       ↓
┌──────────────────────────┐
│ 3. 系统描述生成            │
│ SystemModelAssembler      │
└──────┬───────────────────┘
       │ [新增] 从 component_smt 读取元件 SMT
       │ [新增] 从 port_config 读取端口配置
       │ [新增] ConnectSugarGenerator
       │        - 根据端口类型选择宏族
       │        - 根据端口数量选择 arity
       │        - 支持 electric/hydraulic/mechanical
       │ ✅ 生成 DEF 块
       │ ✅ 生成 CONNECT 块
       │ ✅ 保存到 system_smt 表
       ↓
┌──────────────────┐
│ system_smt 表     │ ← 持久化存储
└──────┬───────────┘
       │
       ↓
┌──────────────────┐
│ 4. 功能管理       │
│ FunctionManager  │
└──────┬───────────┘
       │ [改进] 从 system_smt 读取系统描述
       │ [新增] 变量范围配置 UI
       │ [改进] 边界条件自动识别
       │ [改进] 约束完整性检查（执行器取反）
       │ ✅ 保存到 function_definition 等表
       ↓
┌──────────────┐
│ 5. D矩阵构建  │
│ DMatrixBuilder│
└──────┬───────┘
       │ [改进] 从 system_smt 读取系统描述
       │ [改进] 从 function_definition 读取功能
       │ [新增] 扩展维度（费用、时间、严重度）
       │ ✅ 保存到 dmatrix_meta
       │ [新增] 保存到 test_definition 等表
       ↓
┌──────────────────┐
│ 6. 测试管理集成   │
│ TestManagement   │
└──────┬───────────┘
       │ [新增] 嵌入 DMatrixViewer
       │ [新增] 连接测试优选
       │ [新增] 连接指标预计
       │ ✅ 完整闭环
       ↓
┌──────────────┐
│ 7. 诊断决策树  │
└──────────────┘
```

## 关键组件依赖关系

```
┌─────────────────────────────────────────────────────────┐
│                   SystemModelAssembler                   │
│                   [缺失 - P0 优先级]                     │
└────────┬─────────────────────────────┬──────────────────┘
         │                             │
         │ 依赖                         │ 依赖
         ↓                             ↓
┌────────────────────┐      ┌────────────────────────┐
│ContainerBehavior   │      │ConnectSugarGenerator   │
│Adapter             │      │[缺失 - P0 优先级]      │
│[缺失 - P0 优先级]  │      └────────┬───────────────┘
└────────┬───────────┘               │
         │                            │ 读取
         │ 读取                       ↓
         ↓                   ┌───────────────────┐
┌─────────────────┐          │port_config        │
│component_smt    │          │port_connect_macro │
│[表空 - 未使用]  │          │_family            │
└─────────────────┘          │[有数据 - 未使用]  │
                             └───────────────────┘
```

## P0 任务依赖链

```
第1步: ContainerBehaviorAdapter (3天)
   ↓  实现容器SMT生成
   │  保存到 component_smt 表
   │  提供给 SystemModelAssembler 使用
   │
第2步: ConnectSugarGenerator (3天)
   ↓  实现智能连接宏选择
   │  读取 port_config 和宏族
   │  提供给 SystemModelAssembler 使用
   │
第3步: SystemModelAssembler (5天)
   ↓  组装系统描述
   │  使用上述两个组件
   │  保存到 system_smt 表
   │
第4步: 自动创建容器 (2天)
   ↓  元件插入时触发
   │  调用 ContainerBehaviorAdapter
   │
   ✓  system_smt 表开始有数据
   ↓
[功能管理和D矩阵可以正常工作]
```

## 代码质量评级

```
组件分类                 实现质量    数据完整性   集成度    总评
─────────────────────────────────────────────────────────────
数据库表结构              ★★★★★      ★☆☆☆☆     N/A      ★★★☆☆
  - 表设计优秀，但多数表为空

元件属性编辑 (dialogUnitAttr)
  - T语言编辑              ★★★★★      ★★★★★     ★★★★★    ★★★★★
  - 端口配置               ★★★★☆      ★★★★☆     ★★★★☆    ★★★★☆
  - 校验功能               ★★★★★      ★★★★★     ★★★★★    ★★★★★

本地物料库 (dialogsymbolattribute)
  - T语言编辑              ☆☆☆☆☆      N/A        ☆☆☆☆☆    ☆☆☆☆☆
  - 端口配置               ☆☆☆☆☆      N/A        ☆☆☆☆☆    ☆☆☆☆☆

系统描述构建
  - DEF块生成              ★★★★☆      ★★★★★     ★★☆☆☆    ★★★☆☆
  - 连接语句生成            ★★★☆☆      ★★★★☆     ★☆☆☆☆    ★★☆☆☆
  - 持久化                 ☆☆☆☆☆      ☆☆☆☆☆     ☆☆☆☆☆    ☆☆☆☆☆

容器层
  - 数据模型               ★★★★★      ☆☆☆☆☆     ★★☆☆☆    ★★☆☆☆
  - 行为适配器             ☆☆☆☆☆      N/A        ☆☆☆☆☆    ☆☆☆☆☆

功能管理
  - 基础CRUD               ★★★★☆      ☆☆☆☆☆     ★★★☆☆    ★★★☆☆
  - 依赖分析               ★★★☆☆      ☆☆☆☆☆     ★★☆☆☆    ★★☆☆☆
  - 变量范围配置            ☆☆☆☆☆      N/A        ☆☆☆☆☆    ☆☆☆☆☆
  - 边界识别               ★★☆☆☆      N/A        ★☆☆☆☆    ★☆☆☆☆

D矩阵
  - 构建器                 ★★★★★      ☆☆☆☆☆     ★★★☆☆    ★★★☆☆
  - 查看器                 ★★★★★      ☆☆☆☆☆     ★★★★☆    ★★★★☆
  - 测试管理集成            ☆☆☆☆☆      N/A        ☆☆☆☆☆    ☆☆☆☆☆

校验工具
  - SMT语法检查            ★★★★★      N/A        ★★★★★    ★★★★★
  - 端口一致性检查          ★★★★★      N/A        ★★★★★    ★★★★★
```

## 瓶颈分析

### 🔴 关键瓶颈（阻塞所有后续工作）

1. **system_smt 表为空**
   ```
   当前: 系统描述 → 内存文本 → 消失
   期望: 系统描述 → system_smt 表 → 持久化
   阻塞: 功能管理、D矩阵、所有分析功能
   ```

2. **容器未与实体关联**
   ```
   当前: Equipment ─╳─> container (未关联)
   期望: Equipment ───> equipment_containers ───> container
   阻塞: 容器层建模、系统层分析
   ```

3. **连接宏选择硬编码**
   ```
   当前: 所有连接 → connect2e/3e (硬编码)
   期望: 电气 → connect2e, 液压 → connect2h, 机械 → connect2m
   阻塞: 液压/机械系统建模
   ```

### 🟡 次要瓶颈（影响用户体验）

4. **本地物料库功能缺失**
   ```
   影响: 用户无法在模板库中维护T语言
   解决: 复用 dialogUnitAttr 代码
   ```

5. **功能管理不完整**
   ```
   影响: 功能分析能力受限
   解决: 添加变量范围UI、完善边界识别
   ```

### 🟢 低优先级瓶颈（可延后处理）

6. **D矩阵未集成**
   ```
   影响: 需要在多个窗口间切换
   解决: 嵌入测试管理对话框
   ```

## 修复路径图

```
[开始]
   │
   ├─ 创建 ContainerBehaviorAdapter
   │   ├─ 实现 getContainerSmt()
   │   ├─ 实现 getContainerPorts()
   │   └─ 实现 saveContainerSmt()
   │
   ├─ 创建 ConnectSugarGenerator
   │   ├─ 实现宏族查询
   │   ├─ 实现智能选择（electric/hydraulic/mechanical）
   │   └─ 实现多相数组展开
   │
   ├─ 创建 SystemModelAssembler
   │   ├─ 调用 ContainerBehaviorAdapter.getContainerSmt()
   │   ├─ 调用 ConnectSugarGenerator.generate()
   │   ├─ 组装 DEF/CONNECT/RAW 块
   │   └─ 保存到 system_smt 表
   │
   ├─ 修改元件插入逻辑
   │   ├─ 自动创建容器
   │   ├─ 关联 equipment_containers
   │   └─ 调用 ContainerBehaviorAdapter
   │
   ├─ 修改 LoadProjectSystemDescription
   │   ├─ 调用 SystemModelAssembler.build()
   │   ├─ 保存到 system_smt
   │   └─ 从 system_smt 读取显示
   │
   └─ [第一里程碑达成]
       ↓
   [system_smt 表有数据，后续功能解锁]
```

## 文件清单摘要

### 需要创建的文件 (7个)

```
BO/behavior/
  ├─ systemmodelassembler.h        [P0 - 5天]
  ├─ systemmodelassembler.cpp
  ├─ connectsugargenerator.h       [P0 - 3天]
  └─ connectsugargenerator.cpp

BO/container/
  ├─ containerbehavioradapter.h    [P0 - 3天]
  └─ containerbehavioradapter.cpp

widget/
  └─ (后续阶段创建的UI文件)
```

### 需要修改的文件 (5个)

```
mainwindow_project.cpp
  └─ LoadProjectSystemDescription()    [P0 - 1天]
      └─ 改为使用 SystemModelAssembler

mainwindow.cpp 或元件插入代码
  └─ onEquipmentInserted()             [P0 - 2天]
      └─ 添加容器创建和SMT生成

dialogsymbolattribute.cpp              [P1 - 3天]
  └─ 集成T语言编辑面板

widget/functionmanagerdialog.cpp       [P1 - 2天]
  └─ 从 system_smt 读取系统描述

testability/d_matrix_builder.cpp       [P2 - 3天]
  └─ 扩展维度信息
```

### 优秀可复用的文件 (10个)

```
✅ BO/function/SmtSyntaxChecker.*
✅ BO/function/tmodelvalidator.*
✅ BO/function/functionrepository.*
✅ widget/portconfigeditdialog.*
✅ widget/functionmanagerdialog.*
✅ widget/dmatrixviewerdialog.*
✅ testability/d_matrix_builder.*
✅ BO/systementity.* (Z3求解核心)
✅ dialogUnitAttr.* (元件属性参考)
```

---

**图例说明**:
- ✅ 已实现且质量好
- ⚠️ 部分实现或有问题
- ❌ 未实现或严重缺失
- [P0] 最高优先级（阻塞性）
- [P1] 高优先级（功能完整性）
- [P2] 中优先级（增强功能）

**架构评分**: 基础框架 7/10, 实现完整度 4/10, 集成度 3/10

**关键建议**: 
1. 立即启动 P0 任务，打通 system_smt 持久化
2. 容器层是架构核心，必须优先实现
3. 代码复用需要重视，避免重复劳动
